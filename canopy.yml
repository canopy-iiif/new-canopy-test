collection:
  uri: https://api.dc.library.northwestern.edu/api/v2/search?query=%28joan+baez%29&size=40&as=iiif
iiif:
  # Max number of manifests to process per chunk
  chunkSize: 20
  # Concurrent fetch/build workers per chunk
  concurrency: 3
